import 'package:flutter/material.dart';

class Segundap extends StatelessWidget {
  const Segundap({super.key});

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: const Text("cvgwdc"),

      ),
      body: Center(
        child: Text('Já está na segunda página')
      ),

    );
  }
}
